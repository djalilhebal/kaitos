/**
 * It is a "span" that contains a "spoiler". The user can select it to reveal it.
 *
 * @example
 * In Attack on Titan S3,
 * <SpoilerSpan>
 *   some spoiler
 * </SpoilerSpan>.
 */
export default function SpoilerSpan({children}) {
    // TODO
    return (
        <span>
            {children}
        </span>
    );
}
